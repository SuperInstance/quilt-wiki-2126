# ANCHOR F2 — The Hearth Loop ← its 2026 seeds

*This is the 2026 seed; here is what exists, here is the gap to 2126.*

## The 2026 seeds

- **`quilt-verilog/rtl/q_hebb_edge.v`** (`/home/eileen/projects/quilt-verilog`) — **the edge trains itself.** Hebbian cofire training in RTL: when an effect arrives on a linked edge and the cell is active, the edge's weight grows — the substrate literally learns from the signal it carries, verified by the formal suite. This is F2's loop in silicon: the signal changes the medium, the medium changes the signal.
- **`elephant`** (`/home/eileen/projects/elephant`) — rooms *acclimate*: the field drifts toward the ensemble of dial readings, and the drifted field shapes the next reading. A room that trains itself under its own conversation — the hearth loop's social cousin.

## What exists

Self-modifying weights under signal traffic (Hebbian edges, formally proven); acclimating field dynamics (elephant). Power-law forgetting (`q_hebb_rqh.v`) bounds the write — the 2026 form of "change is only allowed if the light pays for it."

## The gap to 2126

- The loop's *closure* is host-driven: events arrive, edges learn; but the learned state does not steer the source of the events. The lantern's lamp bends the glass that bends the lamp — a closed physical loop. The 2026 loop is open at exactly that joint.
- Photorefractive two-wave mixing (LiNbO₃) is real 1980s physics but no fleet repo touches it. The glass seasoning remains fiction.
- The hearth rule's *gate* (stop writing when the answer is right — see [examples/hearth_loop.py](../examples/hearth_loop.py)) has no silicon analog: Hebbian edges always write.

> The 2026 edge learns from what passes through it. The 2126 loaf learns toward what should pass through it. That preposition is a hundred years.
